Compilation commands on M1 Mac
```
mkdir build && cd build
cmake -GNinja -B . -S ../llvm-project/llvm -DLLVM_ENABLE_PROJECTS="mlir" -DLLVM_TARGETS_TO_BUILD="ARM;X86;AArch64" -DMLIR_INCLUDE_INTEGRATION_TESTS=ON -DLLVM_ENABLE_ASSERTIONS=ON -DBUILD_SHARED_LIBS=ON -DLLVM_INCLUDE_UTILS=ON -DLLVM_INSTALL_UTILS=ON -DLLVM_BUILD_EXAMPLES=ON -DMLIR_ENABLE_BINDINGS_PYTHON=ON -DPython3_EXECUTABLE="$(which python3)" -DCMAKE_BUILD_TYPE="DEBUG" -DLLVM_EXTERNAL_PROJECTS="CVM" -DLLVM_EXTERNAL_CVM_SOURCE_DIR="$(git rev-parse --show-toplevel)"
```
